import { Format } from "../Helpers/Format";
import { ParameterData } from "../Helpers/ParameterData";
declare class Parameters {
    protected _parameters: {};
    set(parameters: ParameterData): void;
    private sort;
    get all(): ParameterData;
    static fromBasics(user: string, version: string, format?: Format): Parameters;
}
export { Parameters };
