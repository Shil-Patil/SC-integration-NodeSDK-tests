import { BaseManager } from "./BaseManager";
import { Document } from "../Model/Document/Document";
declare class DocumentManager extends BaseManager {
    getDocument(documentType: string, orderItemsIds: number[]): Promise<Document>;
}
export { DocumentManager };
