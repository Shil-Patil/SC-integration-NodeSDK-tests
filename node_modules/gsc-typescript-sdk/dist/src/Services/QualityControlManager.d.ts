import { QualityControls } from "../Model/QualityControl/QualityControls";
import { BaseManager } from "./BaseManager";
declare class QualityControlManager extends BaseManager {
    static readonly DEFAULT_LIMIT = 100;
    static readonly DEFAULT_OFFSET = 0;
    protected getQcStatus(limit: number, offset: number, SellerSKU?: string[]): Promise<QualityControls>;
    getAllQcStatus(limit?: number, offset?: number): Promise<QualityControls>;
    getQcStatusBySkuSellerList(skuSellerList?: string[], limit?: number, offset?: number): Promise<QualityControls>;
    protected setListDimensions(limit: number, offset: number): {
        verifiedLimit: number;
        verifiedOffset: number;
    };
}
export { QualityControlManager };
