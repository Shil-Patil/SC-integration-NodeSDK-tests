import { BaseManager } from "./BaseManager";
import { ProductManagerInterface } from "./Contract/ProductManagerInterface";
import { FeedResponse } from "../Response/FeedResponse";
import { Products } from "../Model/Product/Products";
import { ProductImages } from "../Types/ProductImages";
declare class ProductManager extends BaseManager implements ProductManagerInterface {
    static readonly DEFAULT_LIMIT = 1000;
    static readonly DEFAULT_OFFSET = 0;
    static readonly DEFAULT_FILTER = "all";
    productCreate(products: Products): Promise<FeedResponse>;
    productUpdate(products: Products): Promise<FeedResponse>;
    addImage(productImages: ProductImages[]): Promise<FeedResponse>;
    private getProducts;
    productRemove(products: Products): Promise<Products>;
    getAllProducts(limit?: number, offset?: number): Promise<Products>;
}
export { ProductManager };
