import { BaseManager } from "./BaseManager";
import { Webhook } from "../Model/Webhook/Webhook";
import { Event } from "../Model/Webhook/Event";
declare class WebhookManager extends BaseManager {
    createWebhook(callbackUrl: string): Promise<string>;
    deleteWebhook(webhookId: string): Promise<void>;
    protected getWebhooks(webhookIds?: string[]): Promise<Array<Webhook>>;
    getAllWebhooks(): Promise<Array<Webhook>>;
    getWebhookByIds(webhookIds: string[]): Promise<Array<Webhook>>;
    protected getWebhookEntities(): Promise<Array<Event>>;
}
export { WebhookManager };
