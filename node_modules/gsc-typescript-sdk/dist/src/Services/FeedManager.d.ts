import { Feed } from "../Model/Feed/Feed";
import { Feeds } from "../Model/Feed/Feeds";
import { BaseManager } from "./BaseManager";
declare class FeedManager extends BaseManager {
    getFeedStatusById(id: string): Promise<Feed>;
    getFeedList(): Promise<Feeds>;
    getFeedOffsetList(offset?: number, pageSize?: number, createdAfter?: Date, updatedAfter?: Date): Promise<Feeds>;
}
export { FeedManager };
