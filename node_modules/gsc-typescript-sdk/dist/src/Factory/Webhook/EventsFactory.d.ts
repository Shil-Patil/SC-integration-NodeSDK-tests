import { Events } from "../../Model/Webhook/Events";
declare class EventsFactory {
    static makeFromWebhook(element: JSON): Events;
    static make(element: JSON): Events;
}
export { EventsFactory };
