declare class JsonStructureValidator {
    static validateStructure(json: JSON, model: string, properties: string[]): void;
    static validateProperty(json: JSON, model: string, property: string): void;
}
export { JsonStructureValidator };
