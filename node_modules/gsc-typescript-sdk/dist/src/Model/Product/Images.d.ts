import { Image } from "./Image";
declare class Images {
    static readonly MAX_IMAGES_ALLOWED = 8;
    protected collection: Image[];
    all(): Image[];
    add(image: Image): void;
    addMany(images: Image[]): void;
    addManyFromUrls(images: string[]): void;
}
export { Images };
