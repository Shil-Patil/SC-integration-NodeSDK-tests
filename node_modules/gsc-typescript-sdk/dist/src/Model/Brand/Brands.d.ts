import { Brand } from "./Brand";
declare class Brands {
    protected collection: Array<Brand>;
    findById(brandId: number): Brand;
    searchByName(name: string): Array<Brand>;
    searchByGlobalIdentifier(globalIdentifier: string): Brand[];
    all(): Array<Brand>;
    add(brand: Brand): void;
}
export { Brands };
