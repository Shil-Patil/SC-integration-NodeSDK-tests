import { CollectionInterface } from "../../Contract/CollectionInterface";
import { Order } from "./Order";
declare class Orders implements CollectionInterface<Order> {
    protected collection: Order[];
    all(): Order[];
    findByOrderId(orderId: number): Order;
    add(order: Order): void;
}
export { Orders };
