import { CollectionInterface } from "../../Contract/CollectionInterface";
import { Event } from "./Event";
declare class Events implements CollectionInterface<Event> {
    protected collection: Event[];
    findByAlias(alias: string): Event;
    all(): Event[];
    add(event: Event): void;
}
export { Events };
