import { CollectionInterface } from "../../Contract/CollectionInterface";
import { Webhook } from "./Webhook";
declare class Webhooks implements CollectionInterface<Webhook> {
    protected collection: Webhook[];
    findByWebhookId(webhookId: string): Webhook;
    all(): Webhook[];
    add(webhook: Webhook): void;
}
export { Webhooks };
