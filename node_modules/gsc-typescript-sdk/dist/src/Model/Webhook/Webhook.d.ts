import { Events } from "./Events";
declare class Webhook {
    protected webhookId: string;
    protected callbackUrl: string;
    protected events: Events;
    protected webhookSource: string;
    constructor(webhookId: string, callbackUrl: string, events: Events, webhookSource?: string);
    getWebhookId(): string;
    getCallbackUrl(): string;
    getEvents(): Events;
    getWebhookSource(): string;
}
export { Webhook };
