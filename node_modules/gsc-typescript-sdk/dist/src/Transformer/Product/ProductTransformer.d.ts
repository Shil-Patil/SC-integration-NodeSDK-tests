import { ProductAttributes } from "../../Helpers/ProductAttributes";
import { Category } from "../../Model/Category/Category";
import { ProductInterface } from "../../Model/Product/Contract/ProductInterface";
declare class ProductTransformer {
    static asXml(product: ProductInterface): JSON;
    static addAttributes(productJson: any, attributes: ProductAttributes): void;
    static attributeAsString(attribute: unknown): string | number;
    static attributeObjectAsString(attribute: unknown): string | number;
    static getCategoriesAsString(categories: Category[]): string;
    static imagesAsXml(product: ProductInterface): JSON;
    static skuAsXml(product: ProductInterface): JSON;
}
export { ProductTransformer };
